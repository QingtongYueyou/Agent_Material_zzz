# Source Assets

Put raw source splat files here.

- Supported inputs: `.ply`, `.spz`, `.splat`, `.ksplat`
- The auto-ingest pipeline will register these files and build runtime variants when possible.
- Dropping a raw file into `static/splat_files/` root is still supported; the sync job will move it here automatically.
