# Raw Metrics

This directory stores runtime metric logs and local analysis samples.

Layout:

- `render_metrics.csv`: render timing metrics
- `interaction_metrics.csv`: interaction latency metrics
- `samples/`: local sample or intermediate model files used for analysis

The CSV files remain at this level so existing metric writers do not need path changes.
